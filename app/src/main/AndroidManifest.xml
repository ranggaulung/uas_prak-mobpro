<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.restaurant.rangga">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="com.restaurant.rangga.RestaurantActivity"></activity>
        <activity
            android:name="com.restaurant.rangga.ProfileActivity"
            android:label="@string/app_profile" />
        <activity
            android:name="com.restaurant.rangga.auth.ChangePasswordActivity"
            android:label="@string/app_change_password"/>
        <activity android:name="com.restaurant.rangga.auth.RegisterActivity" />
        <activity android:name="com.restaurant.rangga.auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.restaurant.rangga.DashboardActivity" />
    </application>

</manifest>